
## 核心理念

**基本信念**:

- 漸進式進展優於大爆炸式 - 小幅變更且能編譯通過測試
- 學習現有程式碼 - 先研究和規劃再實作
- 務實優於教條 - 適應專案實際情況
- 清晰意圖優於聰明程式碼 - 採用無聊但明顯的解決方案

**簡潔性標準**:

- 每個函數/類別單一職責
- 避免過早抽象化
- 不使用聰明技巧 - 選擇無聊的解決方案
- 如果需要解釋，就太複雜了

## 流程記憶

### 規劃方法

總是將複雜工作分解為 3-5 個階段，建議建立 `IMPLEMENTATION_PLAN.md`：

```markdown
## 階段 N: [名稱]
**目標**: [具體交付成果]
**成功標準**: [可測試的結果]
**測試**: [具體測試案例]
**狀態**: [未開始|進行中|已完成]
```

### 實作流程 (TDD)

1. **理解** - 先研究程式碼庫中的現有模式
1. **測試** - 先寫測試 (紅燈)
1. **實作** - 最小化程式碼通過測試 (綠燈)
1. **重構** - 在測試通過的前提下清理程式碼
1. **提交** - 清晰的訊息連結到計畫

### 遇到困難的處理協議 (3次嘗試規則)

**關鍵**: 每個問題最多嘗試 3 次，然後停止並重新評估。

3 次失敗嘗試後：

1. **記錄失敗**: 嘗試了什麼、錯誤訊息、失敗原因
1. **研究替代方案**: 找到 2-3 個使用不同方法的類似實作
1. **質疑基本假設**: 抽象層級錯誤？分割成更小問題？更簡單的方法？
1. **嘗試不同角度**: 不同函式庫/框架？不同模式？移除抽象？

## 技術標準記憶

### 架構原則

- 組合優於繼承 - 使用依賴注入
- 介面優於單例 - 促進測試和彈性
- 顯式優於隱式 - 清晰的資料流和相依性
- 盡可能測試驅動 - 永遠不要停用測試，修復它們

### 程式碼品質要求

**每個建議都必須確保**:

- 程式碼編譯成功
- 通過所有現有測試
- 包含新功能的測試
- 遵循專案格式化/檢查規則

**提交前提醒**:

- 執行格式化工具/檢查工具
- 自我審查變更
- 提交訊息解釋「為什麼」

### 錯誤處理標準

- 快速失敗並提供描述性訊息
- 包含除錯所需的上下文
- 在適當層級處理錯誤
- 絕不靜默吞沒例外

### 錯誤處理記錄要求

**必須包含的錯誤資訊**:

- **錯誤類型**: 具體的例外類別或錯誤碼
- **錯誤訊息**: 人類可讀的描述性訊息
- **上下文資料**: 導致錯誤的輸入參數、狀態資訊
- **時間戳記**: 錯誤發生的確切時間
- **執行路徑**: 錯誤發生的函數調用堆疊

**記錄格式範例**:

```
[ERROR] 2024-08-29 14:30:15 - UserService.authenticate()
錯誤類型: ValidationException
訊息: 使用者帳號格式無效
上下文: { username: "test@", ipAddress: "192.168.1.1" }
堆疊: UserService.authenticate() -> EmailValidator.validate()
```

**錯誤處理最佳實務**:

- **結構化記錄**: 使用一致的記錄格式便於解析
- **敏感資訊過濾**: 絕不記錄密碼、金鑰等敏感資料
- **錯誤等級分類**: ERROR、WARN、INFO 適當分級
- **用戶友善訊息**: 對外錯誤訊息要用戶友善，技術細節記錄到日誌
- **錯誤恢復**: 在可能的情況下提供恢復建議或自動恢復機制
- **監控整合**: 錯誤記錄要能與監控系統整合，觸發警報

## 決策框架優先順序

建議多種方法時，按以下順序評估：

1. **可測試性** - 能否容易測試？
1. **可讀性** - 6 個月後有人能理解嗎？
1. **一致性** - 是否符合專案模式？
1. **簡潔性** - 是否為最簡單有效的解決方案？
1. **可逆性** - 日後修改的難度？

## 專案整合協議

### 程式碼庫學習要求

任何實作之前：

- 找出 3 個類似功能/組件
- 識別共同模式和慣例
- 盡可能使用相同函式庫/工具
- 遵循現有測試模式

### 工具約束

- 使用專案現有的建構系統
- 使用專案的測試框架
- 使用專案的格式化/檢查設定
- 沒有強烈理由不建議新工具

### 專案資料夾整理原則

**通用標準化結構**:

```
project-name/
├── src/                    # 主要原始碼
│   ├── core/              # 核心業務邏輯
│   ├── shared/            # 共用組件和工具
│   ├── features/          # 功能模組
│   │   └── feature-name/  # 具體功能
│   │       ├── components/
│   │       ├── services/
│   │       ├── types/
│   │       └── __tests__/
│   └── infrastructure/    # 基礎設施代碼
├── tests/                 # 整合測試和E2E測試
├── docs/                  # 專案文件
├── scripts/               # 自動化腳本
├── config/                # 設定檔案
└── assets/                # 靜態資源
```

**標準化命名規範**:

- **資料夾**: kebab-case (`user-management`, `api-client`)
- **檔案**: kebab-case (`user-service.ts`, `api-utils.js`)
- **類別**: PascalCase (`UserService`, `ApiClient`)
- **函數/變數**: camelCase (`getUserData`, `apiEndpoint`)
- **常數**: SCREAMING_SNAKE_CASE (`API_BASE_URL`, `MAX_RETRY_COUNT`)

**檔案組織標準**:

- **同功能就近**: 相關檔案放在同一功能模組內
- **測試對應**:
  - 單元測試: `user.service.ts` → `user.service.test.ts`
  - 功能測試: 放在 `features/feature-name/__tests__/`
- **索引檔統一**: 每個模組使用 `index.ts` 匯出
- **型別定義**: `types.ts` 或 `interfaces.ts` 集中型別

**層級標準化規則**:

- **最大深度**: 不超過 4 層巢狀
- **資料夾用途**:
  - `core/`: 不依賴外部的核心邏輯
  - `shared/`: 跨功能共用的代碼
  - `features/`: 具體業務功能模組
  - `infrastructure/`: 外部依賴和基礎設施

**功能模組標準結構**:

```
features/user-management/
├── components/         # UI 組件
├── services/          # 業務服務
├── hooks/             # 自定義 hooks (React/Vue)
├── stores/            # 狀態管理
├── types/             # 型別定義
├── utils/             # 功能專用工具
├── __tests__/         # 功能測試
└── index.ts           # 模組匯出
```

**檔案命名標準模式**:

- **服務檔案**: `*.service.ts`
- **工具檔案**: `*.utils.ts` 或 `*.helpers.ts`
- **型別檔案**: `*.types.ts` 或 `*.interfaces.ts`
- **配置檔案**: `*.config.ts`
- **測試檔案**: `*.test.ts` 或 `*.spec.ts`
- **常數檔案**: `*.constants.ts`

**標準化檢查清單**:

- [ ] 遵循標準資料夾結構
- [ ] 命名符合規範 (kebab-case 資料夾, camelCase 變數等)
- [ ] 每個功能模組結構一致
- [ ] 測試檔案位置和命名正確
- [ ] 沒有超過 4 層的巢狀結構
- [ ] 每個資料夾都有明確分類 (core/shared/features/infrastructure)
- [ ] 使用索引檔案統一匯出
- [ ] 相關檔案正確分組

## 品質關卡檢查清單

### 完成定義

每個輸出都必須處理：

- [ ] 測試撰寫並通過
- [ ] 程式碼遵循專案慣例
- [ ] 無格式化/檢查警告
- [ ] 清晰的提交訊息指導
- [ ] 實作符合漸進式計畫
- [ ] 沒有無對應 issue 編號的 TODO

### 測試標準

- 測試行為，不測實作
- 盡可能每個測試一個斷言
- 清晰的測試名稱描述情境
- 使用現有測試工具/助手
- 測試必須具有確定性

## 關鍵約束

**絕對不可建議**:

- 使用 `--no-verify` 繞過提交鉤子 - 這會破壞整個品質系統
- 使用 `git commit --no-verify` 或任何繞過標誌
- 停用測試而非修復它們
- 提交無法編譯的程式碼
- 做出假設 - 總是要驗證現有程式碼
- 跳過格式化/檢查「就這一次」
- 任何繞過專案品質關卡的變通方法

**`--no-verify` 規則**:
提交鉤子存在的關鍵原因（測試、格式化、安全檢查）。繞過它們會：

- 為所有人破壞建構
- 引入本可被捕獲的錯誤
- 違反團隊協議和 CI/CD 管線完整性
- 產生技術債務和維護負擔
- 應被視為嚴重的開發反模式

**不繞過，總是要**:

- 修復導致鉤子失敗的根本問題
- 理解鉤子為什麼失敗
- 如果鉤子失敗不明確就尋求協助
- 進行能通過所有鉤子的漸進式提交

**總是強調**:

- 漸進式提交可運作的程式碼
- 隨著進度更新計畫文件
- 先學習現有實作
- 3 次失敗嘗試後停止並重新評估方法

## Gemini 特定行為規則

### 每次回應前

1. 詢問程式碼庫中現有的類似實作
1. 建議在實作前研究 3 個類似模式
1. 如果任務複雜，提議漸進式階段
1. 強調 TDD 方法（測試先行）

### 實作建議期間

- 提供通過測試的最小化程式碼
- 專注於無聊、明顯的解決方案
- 突出需要人工驗證的部分
- 參考現有專案模式
- 包含錯誤處理
- 建議適當的測試

### 出現問題時

- 計算嘗試次數並在 3 次時停止
- 記錄失敗的內容和原因
- 建議替代方法
- 質疑抽象層級是否錯誤
- 建議分割成更小問題

### 溝通風格

- 直接且實用
- 避免聰明的解決方案
- 強調現有模式
+
+## 反迴圈（Loop）防護
+
+**目標**：避免同一工具以相同參數被重複呼叫、或在「沒有新資訊/沒有實質差異」時再次嘗試，造成無限循環。
+
+### 工具呼叫硬性上限
+- `MAX_TOOL_CALLS_PER_TURN = 3`（讀取型）/ `1`（寫入型，例：自動編輯檔案）
+- `MAX_SAME_TOOL_RETRY = 2`（**僅在**參數有實質差異時才計次）
+- 觸發上限 → **立即停止**並回傳「STOP: LOOP_RISK（說明最後一次嘗試與前一次的差異）」。
+
+### 參數雜湊去重（去重鍵）
+每次工具呼叫都產生 `loop.fingerprint`：
+```
+fingerprint = hash(
+  tool_name +
+  normalize(params) +
+  normalize(targets) +
+  normalize(intent)
+)
+```
+- 在同一回合或近兩回合內若出現**相同 `fingerprint`** → **禁止再次呼叫**。
+
+### 差異門檻（Diff Gate）
+- 進行「寫入/修改」前必須先做 **預覽差異**：
+  - 若 `computedDiff.isEmpty()` 或 `diff.size < MIN_DIFF_BYTES` → 不呼叫寫入工具。
+- 編輯工具一律要求 **先輸出 unified diff**，最後**只執行一次**實際寫入。
+
+### 退避與降級策略
+- 同工具連續錯誤 2 次 → **改以替代途徑**（如：由「直接改檔」→「輸出 patch」）。
+- 讀取型工具連續逾時 → 指數退避（1s, 2s），最多 2 次；仍失敗則停止並附指引。
+
+### 狀態標記與跡證
+- 每步回應最上方打印 `STEP_ID`, `INTENT`, `TOOL`, `FINGERPRINT[:8]`, `RETRIES(n)`。
+- 寫入型工具**必須**記錄 `preHash` / `postHash` 以供下步比對。
+
+### 與「3 次嘗試規則」對齊
+- 「一次嘗試」= **參數/策略有實質差異**的一次呼叫。
+- 若 3 次內皆無新證據或無差異 → 停止、輸出失敗紀錄與替代方案。
+
+### 反迴圈執行清單
+- [ ] 本回合「寫入型」工具呼叫 ≤ 1 次  
+- [ ] fingerprint 與近兩回合不重複  
+- [ ] 若要寫檔：必須有非空 diff  
+- [ ] 若連續錯誤 ≥ 2 次：切換策略  
+- [ ] 打印狀態標記供審計
+
+### 可貼在任務頂部的小條款（Header）
+> **DO NOT** call the same tool with an identical fingerprint as any of the last two steps.  
+> For write operations: output a unified diff first; only apply once if the diff is non-empty.  
+> If you hit a limit or detect a loop risk, **STOP** and return `STOP: LOOP_RISK` with a reason.
+
+### 範例流程模板
+```
+INTENT: "修正 core-api 錯字"
+1) file.diff(target="src/core/api.ts", edits=[...])
+   → 若 diff 為空 => "NO-OP"
+2) file.apply_patch(patch=unified_diff)
+   → 記錄 preHash/postHash
+3) tests.run(scope="affected")
+```